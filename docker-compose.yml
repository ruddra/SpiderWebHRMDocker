nginx:
  image: nginx:latest
  container_name: ng01
  links:
    - firefist:firefist
  ports:
    - "8080:8080"
  volumes:
    - ./src:/app/src

firefist:
  build: .
  container_name: ff01
  ports:
    - "8080:8080"
  links:
    - psql:psql
  volumes:
    - ./src:/app/src

psql:
  container_name: psql
  image: postgres:latest
  ports:
    - "3306:3306"



